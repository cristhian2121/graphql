type Course {
    _id: ID!,
    title: String!,
    teacher: String!,
    description: String!,
    topico: String
}

type Query {
    "Return Courses"
    getCourses: [Course]
    "Return a course ! is required"
    getCourse(id: ID!): Course
}